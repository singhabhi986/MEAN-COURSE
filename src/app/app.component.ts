import { Component } from '@angular/core';

//import { Post } from './posts/post-model';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  //title = 'mean-course';
  //REMOVED BELOW BECAUSE WE DONT NEED THEM ANYMORE i.e AFTER SERVICES WAS CREATED SAME DONE IN HTML FILE
  /* storedPosts: Post[] = [];

  onPostAdded(post){
    this.storedPosts.push(post);
  } */
}
